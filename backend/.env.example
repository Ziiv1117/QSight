# Optional: copy to backend/.env. No secrets or API keys are required.
# The launcher loads this file via uvicorn --env-file.
# Use absolute paths; omit variables to use backend/runtime defaults.
# QSIGHT_RUNTIME_DIR=C:/QSightData/runtime
# QSIGHT_DB_PATH=C:/QSightData/runtime/qsight.sqlite3
# QSIGHT_IMAGE_DIR=C:/QSightData/runtime/images
